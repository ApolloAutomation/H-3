substitutions:
  # Manifest bases: stable=Pages, beta=release assets
  stable_manifest_base: "https://apolloautomation.github.io/H-3"
  beta_manifest_base: "https://github.com/ApolloAutomation/H-3/releases/download/beta-fw"
  ota_stable_manifest: "${stable_manifest_base}/firmware/manifest.json"
  ota_beta_manifest: "${beta_manifest_base}/manifest-standard.json"

esphome:
  name: "apollo-h-3"
  friendly_name: Apollo H-3
  comment: Apollo H-3
  name_add_mac_suffix: true
  platformio_options:
    board_build.flash_mode: dio

  project:
    name: "ApolloAutomation.H-3"
    version: "${version}"

  # Hard floor is 2024.6.0; tracks the fleet
  min_version: 2025.11.0
  on_boot:
    # Point update entity at selected manifest
    - priority: -100
      then:
        - script.execute: apply_ota_source
    # H-3 is opt-in for deep sleep
    - priority: 800
      then:
        - lambda: |-
            id(deep_sleep_1).prevent_deep_sleep();

    # Core.yaml's on_value already converts at boot
    - priority: -100
      then:
        - if:
            condition:
              or:
                - binary_sensor.is_on: ota_mode
                - switch.is_on: prevent_sleep
            then:
              - lambda: |-
                  ESP_LOGW("Apollo", "Deep sleep stays disabled (Prevent Sleep on or OTA mode)");
                  id(deep_sleep_1).prevent_deep_sleep();
            else:
              - lambda: |-
                  ESP_LOGI("Apollo", "Deep sleep opted in - enabling automatic sleep");
                  id(deep_sleep_1).allow_deep_sleep();
    # Decoding lives in Core.yaml's wake_decoder
    - priority: -100
      then:
        - script.execute: wake_decoder
    - priority: -10
      then:
        - if:
            condition:
              - lambda: "return id(runTest);"
            then:
              - script.execute: testScript
    # Re-apply Bluetooth Proxy switch after setup
    - priority: -300
      then:
        - if:
            condition:
              switch.is_on: bluetooth_proxy_switch
            then:
              - esp32_ble_tracker.start_scan:
                  continuous: true
            else:
              - esp32_ble_tracker.stop_scan:
  on_shutdown:
    - light.turn_off: body_light
    - light.turn_off: mouth_light
    - light.turn_off: eyes_light
    - light.turn_off: nose_light
    - light.turn_off: hat_light

dashboard_import:
  package_import_url: github://ApolloAutomation/H-3/Integrations/ESPHome/H-3.yaml
  import_full_config: false

deep_sleep:
  sleep_duration: 24h

ota:
  - platform: esphome
    id: ota_esphome
  - platform: http_request
    id: ota_managed

esp32_ble_tracker:
  id: ble_tracker
  # continuous FALSE on purpose; the switch owns scanning
  scan_parameters:
    continuous: false

bluetooth_proxy:
  id: ble_proxy

# Improv over BLE; required for the M4E badge
esp32_improv:
  id: ble_improv
  # 'none' = no physical authorization step
  authorizer: none
  status_indicator: improv_status_led

http_request:
  id: http_request_client
  verify_ssl: true
  # Fits GitHub's ~3.6KB CSP redirect header
  buffer_size_rx: 5120
  # Fits GitHub's long signed redirect URL
  buffer_size_tx: 2048

safe_mode:
  id: safe_mode_component

update:
  - platform: http_request
    id: update_http_request
    name: Firmware Update
    # This one really is firmware
    device_class: firmware
    source: ${ota_stable_manifest}

wifi:
  power_save_mode: HIGH
  output_power: 10
  ap:
    ssid: "Apollo H3 Hotspot"

logger:
  id: logger_component

improv_serial:
  id: serial_improv

captive_portal:
  id: captive_portal_component

web_server:
  id: web_server_component
  port: 80
  version: 3

# Drives hat zone; must be a BinaryOutput
output:
  - platform: template
    type: binary
    id: improv_status_led
    write_action:
      - if:
          condition:
            lambda: "return state;"
          then:
            - light.turn_on:
                id: hat_light
                brightness: 60%
                red: 0%
                green: 40%
                blue: 100%
          else:
            - light.turn_off: hat_light

api:
  id: api_server
  # No key on purpose; M4E forbids baked secrets
  encryption:
  # 'actions' is the current spelling for 'services'
  actions:
    - action: play_buzzer
      variables:
        song_str: string
      then:
        - rtttl.play:
            rtttl: !lambda 'return song_str;'
    - action: play_song_1
      then:
        - script.execute: play_song_1
    - action: play_song_2
      then:
        - script.execute: play_song_2
    - action: play_song_3
      then:
        - script.execute: play_song_3
    - action: play_song_4
      then:
        - script.execute: play_song_4
    - action: play_song_5
      then:
        - script.execute: play_song_5
    - action: play_song_6
      then:
        - script.execute: play_song_6
  reboot_timeout: 0s
  on_client_connected:
    then:
      - delay: 5s
      - if:
          condition:
            or:
              - binary_sensor.is_on: ota_mode
              - switch.is_on: prevent_sleep
          then:
            - lambda: |-
                ESP_LOGW("Apollo", "Preventing Deep Sleep Due To OTA Or Switch");
                id(deep_sleep_1).prevent_deep_sleep();
select:
  - platform: template
    name: "Firmware Channel"
    id: firmware_channel
    icon: mdi:source-branch
    entity_category: "config"
    optimistic: true
    restore_value: true
    options:
      - "Stable"
      - "Beta"
    initial_option: "Stable"
    on_value:
      then:
        - script.execute: apply_ota_source

button:
  # Renamed: the update: entity already uses that name
  - platform: template
    name: "Reinstall Firmware"
    id: update_firmware
    icon: mdi:cloud-download
    entity_category: "config"
    on_press:
      - logger.log: "Applying firmware update for the selected channel"
      - delay: 3s
      - script.execute: apply_ota_source
      - script.wait: apply_ota_source
      # Give async manifest fetch time to land
      - delay: 5s
      - lambda: id(update_http_request).perform(true);

  # Recovery entities; support asks for these first
  - platform: restart
    name: "Restart"
    id: restart_button
    entity_category: "diagnostic"

  - platform: safe_mode
    name: "Restart (Safe Mode)"
    id: safe_mode_button
    entity_category: "diagnostic"

switch:
  - platform: template
    name: "Bluetooth Proxy"
    id: bluetooth_proxy_switch
    icon: mdi:bluetooth
    entity_category: "config"
    restore_mode: RESTORE_DEFAULT_OFF
    optimistic: true
    on_turn_on:
      - esp32_ble_tracker.start_scan:
          continuous: true
    on_turn_off:
      - esp32_ble_tracker.stop_scan:

  - platform: template
    name: "Prevent Sleep"
    id: prevent_sleep
    icon: mdi:sleep
    restore_mode: RESTORE_DEFAULT_ON
    optimistic: true
    entity_category: "config"
    on_turn_on:
      then:
        - lambda: |-
            id(deep_sleep_1).prevent_deep_sleep();
    on_turn_off:
      then:
        - if:
            condition:
              binary_sensor.is_off: ota_mode
            then:
              - lambda: |-
                  id(deep_sleep_1).allow_deep_sleep();

binary_sensor:
  - platform: status
    name: Online
    id: ink_ha_connected
  - platform: homeassistant
    name: "OTA Mode"
    id: ota_mode
    # Mirrors an HA helper; never dashboard material
    entity_category: "diagnostic"
    entity_id: input_boolean.apollo_ota_mode
    on_press:
      then:
        - lambda: |-
            id(deep_sleep_1).prevent_deep_sleep();
    on_release:
      then:
        - if:
            condition:
              switch.is_off: prevent_sleep
            then:
              - lambda: |-
                  id(deep_sleep_1).allow_deep_sleep();

sensor:
  - platform: wifi_signal
    name: RSSI
    id: wifi_signal_db
    update_interval: 60s
    entity_category: "diagnostic"

# Diagnostics, all disabled by default
text_sensor:
  - platform: wifi_info
    ip_address:
      name: "IP Address"
      id: wifi_ip_address
      entity_category: "diagnostic"
      disabled_by_default: true
    ssid:
      name: "Connected SSID"
      id: wifi_ssid
      entity_category: "diagnostic"
      disabled_by_default: true
    bssid:
      name: "Connected BSSID"
      id: wifi_bssid
      entity_category: "diagnostic"
      disabled_by_default: true
    mac_address:
      name: "MAC Address"
      id: wifi_mac_address
      entity_category: "diagnostic"
      disabled_by_default: true

  - platform: version
    name: "ESPHome Version"
    id: esphome_version
    entity_category: "diagnostic"
    disabled_by_default: true

script:
  - id: apply_ota_source
    # Set OTA manifest from the channel select
    then:
      - lambda: |-
          const bool beta = id(firmware_channel).current_option() == "Beta";
          std::string url = beta ? "${ota_beta_manifest}" : "${ota_stable_manifest}";
          ESP_LOGI("firmware", "OTA manifest set to: %s", url.c_str());
          id(update_http_request).set_source_url(url);
      - component.update: update_http_request

  - id: statusCheck
    then:
      - if:
          condition:
            - lambda: 'return id(ink_ha_connected).state;'
          then:
            - logger.log: "Apollo Automation: Connected To HA"
            - light.turn_on:
                id: body_light
                brightness: 40%
                red: 0%
                green: 0%
                blue: 100%
          else:
            - if:
                condition:
                  - wifi.connected
                then:
                  - logger.log: "Apollo Automation: Connected To Wifi"
                  - light.turn_on:
                      id: body_light
                      brightness: 40%
                      red: 0%
                      green: 100%
                      blue: 0%
                else:
                  - logger.log: "Apollo Automation: Not Connected To Wifi"
                  - light.turn_on:
                      id: body_light
                      brightness: 40%
                      red: 100%
                      green: 100%
                      blue: 0%
      - delay: 5s
      - light.turn_off: body_light
  - id: ShouldSleep
    then:
      - if:
          condition:
            or:
              - binary_sensor.is_on: ota_mode
              - switch.is_on: prevent_sleep
          then:
            - lambda: |-
                ESP_LOGW("Apollo", "Preventing Deep Sleep Due To OTA On Boot");
                id(deep_sleep_1).prevent_deep_sleep();
          else:
            - deep_sleep.enter:
                id: deep_sleep_1
  # Same guards as prevent_sleep on_turn_off
  - id: resumeSleep
    then:
      - if:
          condition:
            and:
              - binary_sensor.is_off: ota_mode
              - switch.is_off: prevent_sleep
          then:
            - lambda: |-
                id(deep_sleep_1).allow_deep_sleep();


packages:
  core: !include Core.yaml
